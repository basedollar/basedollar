# RPC URL for Base network
RPC_URL=https://mainnet.base.org

# Subgraph URL for querying trove data
SUBGRAPH_URL=https://api.studio.thegraph.com/query/YOUR_SUBGRAPH_ID

# AeroManager contract address
AERO_MANAGER_ADDRESS=0x...

# Distribution period in seconds (default: 7 days = 604800)
DISTRIBUTION_PERIOD_SECONDS=604800

# Block to start fetching events from (AeroManager deployment block)
START_BLOCK=0
